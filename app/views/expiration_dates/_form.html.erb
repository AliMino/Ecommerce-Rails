<%= form_with(model: expiration_date, local: true) do |form| %>
  <% if expiration_date.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(expiration_date.errors.count, "error") %> prohibited this expiration_date from being saved:</h2>

      <ul>
      <% expiration_date.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.datetime_select :date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
